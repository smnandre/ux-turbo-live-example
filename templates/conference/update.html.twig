{% extends 'base.html.twig' %}

{% block content %}

    <h1>Update Conference</h1>

    <section>

        <article>
            <turbo-frame id="conference_card_form_{{ conference.id }}">
                {{ block('card_form', 'conference/_card.html.twig') }}
            </turbo-frame>
        </article>

    </section>

{% endblock %}
